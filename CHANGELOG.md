# 更新日志

本项目遵循语义化的版本号；所有名称、文本、数值、设定均为《零环》原创。

## 0.3.1 —— 新怪与修复

### 新增怪物（10 → 20 种）
- 新增 10 种原创怪物，并为「冰封 / 混乱 / 恐惧 / 灼烧 / 迟缓 / 易伤」补上**实战来源**（点亮状态对玩家的影响）：环窟豺、尖啸蝠（恐惧）、霜噬蛞蝓（冰封）、迷瘴菌伞（混乱）、缚根藤怪（迟缓）、熔皮蜥（灼烧）、锈蚀甲虫（易伤）、虚空独眼（远程·混乱）、盐晶巨像、噬魂游影（穿墙·恐惧）。
- 新增 `MonsterDef.onHit`（命中附带状态）与 `minDepth`（出现层数）；刷新池改为按 `minDepth` 过滤（深度越深种类越多），不再受旧「前 N 种」上限限制。图鉴提示新增命中特性（冰击 / 惑击 / 啸击…）。

### 修复
- **主动攻击的怪物不再逃跑**：仅偷金币的铜牙鼠会逃；裂骨犬等近战攻击者残血也死战到底。
- **穿墙怪死在墙里的掉落可拾取**：掉落改为定位到最近的可达地板（穿墙 BFS），不再卡在墙内无法拾取。

### 平衡
- 零环守卫二阶段攻击加成 +3 → +2；断链狂徒低血回复 power 2 → 1（集中到 `GameScene` 顶部常量块）。

## 0.3.0 —— 规则互锁

不再盲目堆内容，而是让既有系统**互相影响**，让每回合产生更多有效决策。**完全兼容 0.1 / 0.2 旧存档**（读取时自动迁移、缺字段补默认）。

### 玩家向
- **更严格的回合**：装备 / 卸装 / 丢弃 / 购买 / 开宝箱等所有改变世界的动作都消耗一回合；打开界面、取消、查看不消耗；失败的「已发出」动作（射空、踹门失败、拆陷阱失败）也消耗。
- **状态会影响你**：冰封让动作失败、迟缓让敌人多动一轮、混乱打乱方向、恐惧削弱近战、易伤增伤、回复持续回血——且**存档不再洗掉负面状态**。
- **怪物行为更鲜明**：偷金怪偷完就逃、击杀夺回；爆炸怪进视野有预警；分裂怪不再刷经验/掉落；守宝怪围着宝藏巡守；穿墙怪不出界。
- **宝箱变成抉择**：撞箱弹出「检查 / 开锁 / 拆陷阱 / 强开 / 打开」，各消耗一回合——先清怪还是赌一手？
- **特殊房间**：锁箱房、陷阱回廊、盐井房、余烬巢、石龛房（祝祷赌注）、商人密室——每层有概率出现，制造局部决策。
- **未知物品更有风险**：新增 4 款负面 / 双刃药剂、3 款双刃卷轴；鉴物卷轴不再一次看穿全背包（普通鉴 1 / 祝福鉴 3 / 诅咒鉴 1+混乱）。
- **装备词缀改变打法**：10 个规则词缀（回声 / 裂骨 / 折光 / 赦链 / 蓄盐 / 断步 / 盗火 / 灯芯 / 血契 / 静步）——溅射、减伤、状态联动、卸诅咒等，不再只是比数字。
- **局外成长横向化**：纵向数值加成收敛并加「纯净模式」开关；新增「图鉴」（记录已击败怪物特性）与「商路」解锁（商人多一格 + 奇货）。

### 开发者向
- **统一动作经济入口** `GameScene.commitPlayerAction(reason)`：turn++ → 玩家状态 tick → 怪物一轮（迟缓再一轮，封顶 2）→ FOV/HUD/存档/死亡检查，各一次；`reason !== 'melee'` 时清空铁拳僧 combo。
- **状态存档**：`StatusSystem.serializeStatuses/restoreStatuses`（turns↔duration、容错跳过未知类型）；玩家状态入 `RunState.playerStatuses`、怪物状态入楼层快照。新增 `blinded` 状态。
- **怪物 trait 闭环**：`Monster` 新增运行时字段（anchor/stolenGold/warning…）并入存档；AI 决策优先级整理于 `AISystem.decideAction`。
- **房间模板系统** `world/RoomTemplates.ts`：生成后认领房间再撒点，连通性洪泛保障；6 个原创模板。
- **规则词缀钩子**：`InventorySystem.hasRule/ruleParam` + `GameScene.applyAffixHit/applyAffixKill`，无事件总线、集中分发。
- **局外成长** `core/Meta.ts`：`metaStatBonus` 集中纵向数值并按上限钳制；`MetaUpgrades` 加横向 `tradeRoutes`、`MetaStats` 加 `seen/version`，迁移保留超额购买。
- **手动测试清单**：见 [`docs/test-plan.md`](docs/test-plan.md)（22 类、覆盖 0.3 全部系统）。

### 平衡微调（0.3.10）
- 零环守卫二阶段攻击加成 +3 → **+2**（避免半血突然过难）。
- 断链狂徒低血回复 power 2 → **1**（抑制续航叠加）。
- 关键数值集中到 `GameScene` 顶部 `0.3.10 balance knobs` 常量块。

## 0.2.0 —— 职业醒来

为环窟带来更接近传统 roguelike 的系统深度。九大职业从此各具灵魂，物品、怪物、地形都有了自己的脾性。**完全兼容 0.1 旧存档**（读取时自动迁移）。

### 动作经济
- 新游戏按职业发放 `startingItems`（缺省时回退到基础药剂 + 干面包），并自动穿戴起始装备。
- 使用药剂 / 食物 / 卷轴、装备 / 卸装、开宝箱、开门、搜索、施法等**成功动作消耗一回合**并触发一轮怪物行动；打开界面不消耗回合。
- 单次动作只触发一轮怪物行动（杜绝重复结算）。

### 职业技能
- 九职业各有专属能力，被动自动生效、主动经底部「技能」键释放，需要方向时弹出方向键（移动端友好）。
- 环骑士「环誓」（每层一次免死 + 易伤）、灰烬医师「急救」、裂隙盗「穿墙」、星盐法师「盐爆」、骨钟祭司「钟鸣」、碎刃游侠「投掷」、铁拳僧「连击」、铜灯旅人「灯火」、断链狂徒「狂怒」。

### 法力与远程
- 新增法力 / 法力上限，随步数缓慢回复，供主动技能消耗。
- 统一的远程攻击流程：点按视线内、射程内的怪物即可远程攻击，**不能穿墙或穿过关闭的门**。
- 远程怪物需有视线才能射击，并会尝试与你保持距离。

### 物品实例、鉴定、祝福 / 诅咒、附魔
- 背包与地面物品升级为 `ItemInstance`（id / defId / 数量 / 已鉴定 / 祝咒 / 附魔 / 充能）。
- 药剂、卷轴默认未鉴定，每局别名固定；使用或「鉴物卷轴」鉴定同类。
- 祝福 / 诅咒影响药剂、卷轴、装备；**被诅咒的装备无法普通卸下**，需「解缚卷轴」。
- 装备附魔影响攻 / 防并显示 +N/−N。新增原创「鉴物卷轴」「解缚卷轴」。

### 装备人偶（11 槽）
- 装备栏由 3 槽扩展为 **11 槽人偶**：主手 / 副手 / 头部 / 肩甲 / 身体 / 腰带 / 手套 / 足部 / 护符 / 戒指 ×2。
- 新增 `GearSlot` 槽位模型（与物品类型解耦）：盾牌入副手、头盔入头部、靴入足部等各归其位；戒指自动填入空闲指位。
- 新增三类原创装备与像素贴图——肩甲、腰带、手套（共 6 件），并进入掉落池。
- 背包界面改为三列人偶布局（左护甲 / 中饰品 / 右护甲与盾）：空槽显示槽名、满槽显示图标。

### 状态效果与抗性
- 统一状态系统：中毒、灼烧、冰封、混乱、恐惧、迟缓、易伤、回复，玩家与怪物皆可携带，每回合结算。
- 抗性系统：毒 / 火 / 冰 / 精神，由职业、怪物种类提供。

### 怪物特性与差异化 AI
- 新增怪物 traits 与 tags，并为多种怪物分配不同特性：低血逃跑、保持距离、毒袭、开门、穿墙、守宝、偷金、死亡分裂、死亡爆裂、迟缓重击。
- 远程怪不再隔墙攻击；Boss 生命低于 50% 时触发一次性的第二阶段。

### 门、宝箱、陷阱与搜索
- 门分敞开 / 关闭 / 上锁：关闭的门阻挡移动与视线，可推开、踹开、长按关门；带 `opensDoors` 的怪物会自行开门。
- 宝箱可能上锁（撬锁）或暗藏机关（开启时触发陷阱）。
- 陷阱改为隐藏 / 显露的独立要素，含尖刺 / 毒气 / 传送 / 索套四类，玩家与怪物都会触发；可「搜索」发现、走向其拆除，铜灯旅人照明可大范围照见。

### 其他
- HUD 显示法力与当前状态；角色面板显示法力、技能（类型 / 耗费 / 剩余次数）、状态与全部已着装槽位。
- 存档版本升至 `v7`，自动迁移 `v5` / `v6`：旧的字符串物品列表转为实例，旧三槽装备（武器 / 防具 / 饰品）按其真实槽位归位（盾→副手、盔→头部……）。

### 修复与打磨
- 修复设置「动画速度」无法选中的问题：背景全屏点击会先于按钮触发把面板关掉；现仅点击面板**之外**才关闭，槽位选择正常生效。
- 修复地牢生成沿墙出现成排门的问题：仅在走廊**真正凿穿**墙处放门（单格缺口），不再产生无意义的长条门。
- 门默认关闭（少数上锁），不再预先敞开；**触摸端点相邻门即可开 / 踹门**，自动寻路会穿过并顺手推开关闭的门。
- 新增**敞开门**贴图，开门后与关闭门视觉可区分（参照宝箱开合）。

## 0.1.0 —— 初窟

- 移动端竖屏、可安装 PWA；五层环窟、回合制移动与战斗、雾视野、宝箱与陷阱、九职业、Boss 与胜负结算、本地存档。所有像素素材由代码生成。
